# build-v3 Database Security Gate

**Status:** ACTIVE · **Owner:** platform · **Last audit:** 2026-07-12 · **DB:** Postgres (Supabase-managed) + self-hosted VPS backend

> **This gate BLOCKS production.** No build-v3 schema, backfill, or backend change reaches prod
> until (a) the automated DB gate `security_gate.sql` passes with zero FAILs, **and** (b) every
> BLOCKING row in the Deployment Gate below is checked with recorded evidence. "Nothing goes to
> prod" is enforced here, not assumed.

The gate has two halves:

| Half                      | What it covers                                                                     | How it runs                                                                          | Blocks         |
| ------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | -------------- |
| **A · Automated DB gate** | RLS, grants, credential exposure, data hygiene — everything provable from the DB   | `psql -v ON_ERROR_STOP=1 -f security_gate.sql` (26 structural + 3 behavioral checks) | the build / CI |
| **B · Deployment gate**   | TLS, SCRAM, pg_hba, role wiring, secrets, pooler contract — the instance & backend | manual/CI checklist below, evidence recorded                                         | the cutover    |

---

## 1 · How this gate was built

A 19-agent adversarial audit (`security-audit-2026-07-12.raw.json`) reviewed the DDL + backfill + live
DB across six dimensions and **empirically reproduced** the critical issues (minted a `password_reset_token`
as `api` with no merchant context; read all 8 password hashes as both `api` and `readonly`; pulled 11
cross-merchant rows out of `conversation_analytics`). It produced **50 confirmed findings** (6 critical, 16
high, 14 medium, 10 low, 4 info), then a red-team pass corrected contradictory fixes and defined the gate
sections below. Everything locally fixable has been fixed and is now asserted by `security_gate.sql`.

**Re-run the audit** whenever the schema, grants, or backfill change materially, and at minimum before each
cutover rehearsal.

---

## 2 · Data-cleaning decisions (reasoned)

The audit's "data-at-rest" dimension drove deliberate cleaning of the backfill. Each decision and its
reasoning:

| Data                                                                            | Decision                                                                                                                                                                                                                                                                                                                    | Reasoning                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Password credentials** (9 users)                                              | Add `umi.user.password_salt` (was a **schema bug** — scrypt is unverifiable without its salt); carry `hash+salt+algorithm` **only** for _unique_ `scrypt-sha256-v1` users; **force-reset** (`invited`, null creds) the 2 `legacy-sha256` and any **shared-hash** (seed-password) accounts; **suspend** the emailless ghost. | The source _did_ have the salt — the audit's "salt gone" premise was a backfill omission. Keep strong working logins; eliminate crackable/shared credential material rather than carrying it. Reset friction is trivial for ~9 internal users.                                   |
| **Historical webhook envelopes** (`outbox` 417, `inbound` 395, `dead_letter` 1) | **Dropped** from the backfill.                                                                                                                                                                                                                                                                                              | Past delivered/processed events are nothing the machine reads back to act → telemetry, not runtime state (the model's own read-back rule) → and their payloads carry raw customer phone/message **PII** into a sealed, unscoped schema. Runtime starts clean; queues regenerate. |
| **Slack channel id/name** in `merchant.audit_log`                               | **Redacted** (`- 'slack_channel_id' - 'slack_channel_name'`).                                                                                                                                                                                                                                                               | `merchant.audit_log` is café-readable (RLS); a Umi-internal Slack id must not leak into it.                                                                                                                                                                                      |
| **Credential columns** on `umi.user`                                            | Kept, but **column-locked** from `api`/`readonly`.                                                                                                                                                                                                                                                                          | Verified only via the worker pool / a SECURITY DEFINER auth function — never on the request path.                                                                                                                                                                                |
| DOB, raw+normalized phone duplication                                           | **Kept** (documented).                                                                                                                                                                                                                                                                                                      | Low-sensitivity CRM attributes; the raw/normalized pair is the model's deliberate "raw truth + derived companion," not a leak.                                                                                                                                                   |

---

## 3 · Automated DB gate — `security_gate.sql`

> **Two gate defects found 2026-07-28 during the UmiPOS integration, both fixed here.**
>
> 1. **`revoke update (col) … from api` had never bound.** Postgres treats a table-level
>    privilege as covering every column, so a column-level REVOKE cannot subtract from the
>    blanket `grant … update … on all tables in schema merchant to api`. The much-commented
>    "UNFORGEABLE" guard on `merchant.contact.normalized_value` was inert for its whole life;
>    only its trigger was protecting the column. The working form (revoke the table UPDATE,
>    grant it back on every other column) is now generated in `90_rls.sql`, and a new gate
>    check asserts the class.
> 2. **Two checks asserted a hardcoded list of table names**, so anything added afterwards was
>    invisible to them and they passed while the new tables went unchecked. Both are now
>    universals. The rule this file already applied to views — _"a hand-maintained list cannot
>    satisfy a universal assertion — it goes stale the first time someone adds a view. It did"_ —
>    applies to every check here, not just that one.

Run: `PGPORT=5233 psql -v ON_ERROR_STOP=1 -d <db> -f security_gate.sql` → must end `SECURITY GATE PASSED`.
All rows below are **enforced by that script** and currently **PASS**.

### Isolation & RLS

- **Every `merchant` base table has RLS _and_ FORCE.** (`relrowsecurity AND relforcerowsecurity` on all 33; FORCE closes the owner-bypass path.)
- **`umi` per-café tables RLS+FORCE:** `subscription, subscription_item, invoice, entitlement_override, user_role` scoped to `app.current_merchant`; global catalogs (`role/permission/channel_type/feature/plan/plan_feature`) stay readable.
- **`runtime` request-path tables RLS+FORCE:** `conversation_cart` and `conversation_turn` (via
  `conversation`), `reminder_sent` (merchant_id), and the POS set — `operator_session`,
  `device_enrollment_challenge`, `elevation_grant`, `security_audit_event`,
  `audit_event_internal`. The last two are INSERT-only for `api`, and still need a policy:
  insert alone is enough to forge an event against another café.
  _(This line named `conversation_state`, which has not existed since it became
  `conversation_cart`. The assertion is now a SWEEP — "every api-reachable runtime table with a
  merchant_id has RLS+FORCE" — rather than a list of names, so the doc going stale can no longer
  make the gate go blind.)_
- **Views run `security_invoker`** (`conversation_analytics`, `effective_entitlement`) and `api` holds **no DML** on any view — the confirmed cross-merchant view leak is closed.
- **Fail-closed GUC:** `umi.current_merchant()` returns NULL on unset/empty context → 0 rows, never an error. Behavioral test proves EGR sees 0 foreign rows; Kalala sees exactly its own.

### Least privilege & credentials

- **`api` cannot read** `umi.user.password_hash` / `password_salt`; **`readonly` cannot** either. `api` keeps identity columns (`email` etc).
- **`api`/`readonly` have zero privilege on the auth substrate** (`runtime.session/otp/password_reset_token/device_session/pairing`).
- **`api` cannot read** `umi.prospect` (Umi sales) or `umi.audit_log` (sealed).
- **`api` is not superuser / not BYPASSRLS**; `worker` is BYPASSRLS (machinery).
- **No default-privilege leak arms `api`** on future tables (explicit grants only).
- **`PUBLIC` cannot CREATE in schema `public`** (CVE-2018-1058 enabler removed); trigger functions have **pinned `search_path`**.
- **Append-only audit:** no role holds UPDATE/DELETE on either `audit_log`.

### Data hygiene

- 0 active users with a NULL hash · 0 `legacy-sha256` retained · ≥1 strong scrypt login survives · ghost `@umi.invalid` not active · no Slack ids in café-readable audit · 0 historical webhook rows in runtime.

---

## 4 · Deployment gate (BLOCKING at cutover — record evidence)

These cannot be asserted from the DDL alone; they are checked at cutover against the running instance/backend.

| #   | Gate                               | Pass criterion                                                                                                                                                                                                                                                                                                                                   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | **Connection role reconciliation** | App pools connect as a role that is `pg_has_role(current_user,'api','member')`, **not** `postgres`/`service_role`; `rolsuper=f, rolbypassrls=f` on the app+readonly pools; only the worker pool is BYPASSRLS. Boot guard aborts otherwise.                                                                                                       | ✅ **Names decided: `api`/`worker` (canonical).** The role is embedded in `DATABASE_URL_APP`/`_WORKER` (env), so cutover points those at LOGIN roles that INHERIT `api`/`worker` (e.g. `api_login`/`worker_login` — `api`/`worker` themselves are NOLOGIN groups you cannot connect as) — an **env change, not code**. ✅ **Boot guard implemented** — `PgService.onModuleInit` → `assertPoolRoles` refuses to boot unless app = non-super/non-BYPASSRLS member of `api` and worker = BYPASSRLS/non-super member of `worker` (attributes read off `current_user`; membership via `pg_has_role(...,'USAGE')`, which passes only when the login role actually **inherits** the group's privileges — a NOINHERIT member is rejected at boot, not silently broken at first query). Unit-tested (`pg.service.spec.ts`). The H1 harness login roles (`api_login`/`worker_login`, `harness-roles.sql`) satisfy it by construction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D2  | **GUC-name contract**              | The backend sets the policy GUC `app.current_merchant` for every request-path transaction.                                                                                                                                                                                                                                                       | ✅ **Resolved (expand-contract):** `pg.service.ts:runWithMerchant` now sets **both** `app.tenant_id` (current prod) **and** `app.current_merchant` (build-v3), transaction-scoped — correct against either schema, zero risk to live prod. **Contract:** drop `app.tenant_id` after cutover.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D3  | **Pooler / SET LOCAL isolation**   | `app.current_merchant` is set via `set_config(..., true)` **inside the same transaction** as the query (never a session `SET`); an integration test proves merchant A's pooled connection cannot read merchant B after reuse.                                                                                                                    | Supabase fronts with Supavisor (transaction pooling). Also: **no merchant-scoped request read may run on the BYPASSRLS worker pool.** ⚠️ **Sanctioned exceptions** (reconcile here, they are not new breaches): the auth substrate is worker-only _by design_ (D11), and the entitlement reads (`AuthRepository.productStatus`, `MerchantsRepository.loadProducts`) run on the worker pool because `umi.*` is SEALED from `api`. Both carry an explicit `merchant_id` predicate. The rule means _no UNSCOPED merchant read_; an explicit-predicate read on worker is the documented pattern. ✅ **Code satisfied + PROVEN.** `runWithMerchant` sets both GUCs with `set_config(..., true)` (transaction-local) inside `BEGIN`/`COMMIT` — `pg.service.ts:266`. `pooler-isolation.integration.ts` asserts it on a REUSED physical connection (same `pg_backend_pid()`): no scope survives a COMMIT, none survives a ROLLBACK, and A never bleeds into B. Red-green verified — flipping that `true` to `false` fails the gate. The file also proves the leak is REAL with a session-scoped `set_config`, so the check is known to be capable of failing. ⚠️ Note recorded in the test: the cross-merchant case does NOT catch this bug (scoping to B overwrites A's leak) — the commit/rollback cases are the load-bearing ones.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D4  | **TLS verify-full (VPS→Supabase)** | Both pools use `ssl:{ca, rejectUnauthorized:true}` (or `sslmode=verify-full&sslrootcert=…`); `pg_stat_ssl` shows `ssl=t` per pool; a wrong CA **fails** the connection; Supabase "Enforce SSL" ON.                                                                                                                                               | Backend currently has no `ssl` option. ✅ **Code half DONE — this cell was stale.** `pg.service.ts:84` sets `ssl:{ca, rejectUnauthorized:true}` on both pools from `PGSSLROOTCERT` (file path or inline PEM; `config.schema.ts:34`, `.env.example:19`), so a wrong CA or hostname FAILS the handshake at connect. `onModuleInit` then reads `pg_stat_ssl` per pool and WARNs (not aborts) on a false report, because a transaction pooler reflects the pooler→db leg. ⏳ **Remaining is provisioning, not code:** put the CA on the VPS, set `PGSSLROOTCERT`, and turn on Supabase "Enforce SSL".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D5  | **SCRAM on login roles**           | `password_encryption=scram-sha-256` (✓ confirmed) **and** every app LOGIN role holds a SCRAM verifier: `pg_shadow.passwd` starts with `SCRAM-SHA-256$`, and none starts with `md5`. Login roles use `INHERIT` membership of `api`/`worker` (**not** `NOINHERIT` — that breaks all access).                                                       | `api`/`worker` are NOLOGIN groups; the real login roles carry the verifiers. 🔴 **The method written here was WRONG and could not run: `pg_shadow` has NO `passwdtype` column.** Its columns are `usename, usesysid, usecreatedb, usesuper, userepl, usebypassrls, passwd, valuntil, useconfig` — the METHOD is the verifier prefix on `passwd`. Corrected 2026-08-14, and the working query now ships in `apps/umi-api/db/roles/004_buildv3_login_roles.sql` §4, proven against a live cluster on both branches (a scram role reads `scram-sha-256 OK`, an md5 role reads `REPROVISION`). ⚠️ `pg_shadow` needs a superuser, so on a managed target this reads NOT READABLE and the operator runs it from the provider console.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D6  | **pg_hba / network**               | Supabase network restrictions limit connections to the VPS egress IP; pg_hba is Supabase-managed (no `trust`/`0.0.0.0` reachable).                                                                                                                                                                                                               | Managed side — record the setting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D7  | **Extensions**                     | **No extension is installed outside `pg_catalog`/`extensions`; no network- or exec-capable extension is installed at all** (`postgres_fdw`, `dblink`, `file_fdw`, `plpython*`, `plperlu`, …); `api`/`worker` have USAGE (not CREATE) on `extensions`; unqualified `vector`/`pg_trgm` ops resolve for `api`. **No list of names** — see the note. | `search_path` = `"$user", public, extensions` (extensions **after** public). ✅ **Checked by `security_gate.sql` (3 checks).** 🔴 **The criterion column named a set of THREE and the target carries SEVEN.** Corrected 2026-08-14: the row no longer names a set. The database also carries `pgcrypto`, `uuid-ossp`, `pg_stat_statements` and — since the location search — `unaccent`, all legitimate. The gate therefore asserts PLACEMENT and CAPABILITY instead of a name list, which does not rot on the next legitimate addition: nothing installed outside `pg_catalog`/`extensions`, no network/exec-capable extension (`postgres_fdw`, `dblink`, `file_fdw`, `plpython*`, `plperlu`, …), and `api`/`worker` hold USAGE but NOT CREATE on `extensions`. All three red-green verified.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D8  | **No FDW/dblink remnants**         | Target DB has **zero** foreign servers / user mappings and no `dblink` credentials carried from the snapshot template.                                                                                                                                                                                                                           | The snapshot template can carry a `postgres_fdw` server embedding the source password. ✅ **Checked by `security_gate.sql`.** Asserts zero foreign servers + zero user mappings + zero FDWs. Zero today, so the check only earns its place after P7's replay — which is exactly when the credential would be left behind. Red-green verified by creating a server and a user mapping in a rolled-back transaction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D9  | **Secret rotation & history**      | Recorded evidence the Supabase JWT secret + Twilio auth token were **rotated** after the leak; `gitleaks`/`trufflehog` over full history returns 0 live secrets.                                                                                                                                                                                 | Standing item — see `cred_exposure` memory.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D10 | **Request-path log redaction**     | `log_statement='none'` and no parameter logging for the request role — tokens/OTP hashes/merchant ids flow as bound params and must not land in logs.                                                                                                                                                                                            | Else logs become a credential sink that survives every grant lockdown. ◑ **Checked by `security_gate.sql` (3 checks), 1 WARN.** ✅ `log_statement = none`. ✅ No role-level `ALTER ROLE … SET log_statement` re-enables it for `api`/`worker`/`readonly` — a cluster-wide 'none' is undone by one such statement, and it is invisible in `pg_settings` read as anyone else. ✅ `log_parameter_max_length_on_error = 0`. ✅ **RESOLVED 2026-08-14 (PR #102) — this row said WARN and no longer does.** `log_parameter_max_length = -1` is the PostgreSQL default, and it means log bind parameters IN FULL. It leaks nothing while no statement is logged, and it becomes a credential sink the moment anyone enables slow-query logging to debug an incident — which is precisely when someone will. **The managed target REFUSED `ALTER DATABASE … SET log_parameter_max_length`** (measured on `xbudk` as `postgres`). A role-level `SET` overrides the cluster default for that role's sessions, so both checks now accept the per-role pin, which is also narrower than the global setting. `apps/umi-api/db/roles/004_buildv3_login_roles.sql` applies it, CI runs that file every round, and the gate reads **44 structural green, 0 acknowledged gaps**. 🔴 Both checks previously used `not exists (… role without the pin …)`, which is TRUE on an empty set — an unprovisioned target read PASS while nothing was measured. They now COUNT the roles and require at least one. ⚠️ **A SQL gate cannot see which role a pool connects as.** Production's worker pool connects as `postgres`, which no name list here covers. `poolLoggingProblem` (`pg.service.ts`) is the authoritative D10 check: it runs at boot, on the real role, and it aborts. |
| D11 | **Auth issuance path**             | Session/OTP/reset-token **issuance** runs only via the worker pool or a SECURITY DEFINER auth function (allowlisted, pinned `search_path`, EXECUTE revoked from PUBLIC); single-use + expiry enforced transactionally (no TOCTOU).                                                                                                               | ✅ **Satisfied today** — every substrate touch (`umi.user` credentials, `runtime.{session,password_reset_token,pairing}`) runs on the **worker** pool (`AuthRepository`, `CustomerSessionService`, `KdsRepository` use `pg.query`/`pg.workerTx`, never `withMerchant`). ✅ **Statically enforced** — `auth-substrate.d11.spec.ts` walks production source (TS AST) and fails CI if any `withMerchant`/`runWithMerchant`/`.app` call site references the substrate; `api` also has no DML on the auth tables (A-gate). No request-path issuance flow exists, so no SECURITY DEFINER function is needed yet — add one (with the transactional single-use guard) only if one appears.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

| D12 | **Second factor on platform grants** | Every live platform grant holder (`umi.user_role`, not revoked, not expired) has a non-null `umi.user.mfa_method`. The method is `totp`: `email_otp` proves possession of nothing and does not satisfy PCI DSS 8.4.1 (`mfa.service.ts`, NIST SP 800-63B §5.1.3.1). | **Blocks the cutover.** One stolen password otherwise reaches every café, and a platform grant holder is the highest-value account in the system. ✅ **Checked by `security_gate.sql`, and the row DECIDES ITS OWN STATUS** (added 2026-08-14, PR #103). It used to carry a written instruction to a person — "change WARN back to FAIL when the MFA module ships" — and the module shipped while the instruction stayed. Three states, each proven against a live database: **PASS** every live holder is enrolled, or there is none; **FAIL** SOME holder is enrolled and another is not, so enrolment demonstrably works here and the gap is a choice; **WARN** NO holder is enrolled and the rollout has not started. ⏳ **Remaining is provisioning, not code:** enrol the platform grant holders. `umi.user.mfa_method` is NULL for every row today. ⚠️ **Enrol only after a client reads `mfaRequired`.** `POST /api/auth/local/login` returns a challenge and NO session for an enrolled account. An older client stores nothing, and that account cannot sign in. The dashboard reads it as of PR #103; `apps/umi-cash` has its own separate login and is unaffected. |

---

## 5 · Accepted / residual risks (documented, not blocking)

- **Outbound-message enqueue** — `api` currently has no `runtime.outbox_event` DML (revoked). Legitimate request-path sends must route through the worker or a definer that stamps the origin `merchant_id`. Until the backend does so, outbound send is worker-only (accepted; documented so it's not silently re-granted).
- **A grant cannot be revoked, only deleted** — `umi.user_role` has no `status` column (the build-v2 `merchant_access.status='active'` filter had nowhere to go), so disabling someone's access means removing the row. Acceptable at this size, but it leaves no audit trail of _why_ access ended and no "suspended" state. Add `status` if access changes ever need to be reviewable.
- **A platform-wide grant is invisible to `api`** — `umi.user_role`'s policy is `merchant_id = umi.current_merchant()`, which a NULL `merchant_id` (the super_admin grant) can never satisfy. Intentional: the auth queries that read it run on the worker pool. Anything reading grants on the request path would silently not see platform grants.
- **RBAC scope not DB-enforced** — `umi.user_role.merchant_id/location_id` is stored; RLS trusts `app.current_merchant`. The single choke-point is the backend setting the GUC from the authenticated grant. A per-policy `session_can_access_merchant()` was considered and **rejected as over-engineering** for a 5-café system (planner cost on every row); revisit if merchant count or role complexity grows.
- **`umi.user` row enumeration** — credentials are column-locked, but `api` can still read identity columns cross-merchant unless routed through the scoped staff join. Low sensitivity; revisit if it becomes a vector.
- **FK existence oracle** — confirmed LOW (random UUIDs; `WITH CHECK` blocks cross-merchant grafts). No action.

---

## 6 · Re-run cadence

- **Every schema/grant/backfill change:** `security_gate.sql` in CI (blocks merge).
- **Before every cutover rehearsal:** full 19-agent audit + Deployment Gate evidence refresh.
- **Raw audit archive:** `security-audit-2026-07-12.raw.json`.
