# ConversaFlow diagnostics

Manual diagnostics and tracing helpers for the shared ConversaFlow platform.

## Purpose

- inspect jobs, logs, turns, deploy state, and traces
- debug live platform behavior without mixing these helpers into the repo root

## Notes

- These scripts are operational helpers, not product runtime code.
- If a new script is for debugging or inspection, place it here instead of the repo root.
